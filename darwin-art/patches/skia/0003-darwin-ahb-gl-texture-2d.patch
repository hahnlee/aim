--- a/src/gpu/ganesh/gl/AHardwareBufferGL.cpp
+++ b/src/gpu/ganesh/gl/AHardwareBufferGL.cpp
@@ -32,0 +33,8 @@
+// ANGLE's Metal backend exposes imported IOSurfaces as 2D EGL images. Its
+// macOS GLES context does not accept GL_TEXTURE_EXTERNAL_OES.
+#if defined(__APPLE__)
+static constexpr GrGLuint kHardwareBufferTextureTarget = GR_GL_TEXTURE_2D;
+#else
+static constexpr GrGLuint kHardwareBufferTextureTarget = GR_GL_TEXTURE_EXTERNAL;
+#endif
+
@@ -43 +51 @@
-            return GrBackendFormats::MakeGL(GR_GL_RGBA8, GR_GL_TEXTURE_EXTERNAL);
+            return GrBackendFormats::MakeGL(GR_GL_RGBA8, kHardwareBufferTextureTarget);
@@ -45 +53 @@
-            return GrBackendFormats::MakeGL(GR_GL_RGBA16F, GR_GL_TEXTURE_EXTERNAL);
+            return GrBackendFormats::MakeGL(GR_GL_RGBA16F, kHardwareBufferTextureTarget);
@@ -47 +55 @@
-            return GrBackendFormats::MakeGL(GR_GL_RGB565, GR_GL_TEXTURE_EXTERNAL);
+            return GrBackendFormats::MakeGL(GR_GL_RGB565, kHardwareBufferTextureTarget);
@@ -49 +57 @@
-            return GrBackendFormats::MakeGL(GR_GL_RGB10_A2, GR_GL_TEXTURE_EXTERNAL);
+            return GrBackendFormats::MakeGL(GR_GL_RGB10_A2, kHardwareBufferTextureTarget);
@@ -51 +59 @@
-            return GrBackendFormats::MakeGL(GR_GL_RGB8, GR_GL_TEXTURE_EXTERNAL);
+            return GrBackendFormats::MakeGL(GR_GL_RGB8, kHardwareBufferTextureTarget);
@@ -54 +62 @@
-            return GrBackendFormats::MakeGL(GR_GL_R8, GR_GL_TEXTURE_EXTERNAL);
+            return GrBackendFormats::MakeGL(GR_GL_R8, kHardwareBufferTextureTarget);
@@ -60 +68 @@
-                return GrBackendFormats::MakeGL(GR_GL_RGBA8, GR_GL_TEXTURE_EXTERNAL);
+                return GrBackendFormats::MakeGL(GR_GL_RGBA8, kHardwareBufferTextureTarget);
@@ -146 +154 @@
-    GrGLuint target = isRenderable ? GR_GL_TEXTURE_2D : GR_GL_TEXTURE_EXTERNAL;
+    GrGLuint target = isRenderable ? GR_GL_TEXTURE_2D : kHardwareBufferTextureTarget;
