--- a/public_libraries.cpp
+++ b/public_libraries.cpp
@@ -1,3 +1,5 @@
+#include "filesystem/guest_config.h"
+#include "filesystem/guest_directory.h"
 /*
  * Copyright (C) 2019 The Android Open Source Project
  *
@@ -97,1 +98,1 @@
-  if (!base::ReadFileToString(configFile, &file_content)) {
+  if (!darwin_art::filesystem::ReadGuestConfig(configFile, &file_content)) {
@@ -108,1 +110,1 @@
-  std::unique_ptr<DIR, decltype(&closedir)> dir(opendir(dirname), closedir);
+  auto dir = darwin_art::filesystem::GuestDirectory::Open(dirname);
@@ -112,1 +114,4 @@
-    while (struct dirent* ent = readdir(dir.get())) {
+    DarwinArtAndroidDirent entry{};
+    while (dir->Next(&entry) == darwin_art::filesystem::GuestDirectory::Result::Entry) {
+      const auto* ent = &entry;
+      // Entry type/name retain Android dirent semantics, not host DIR layout.
@@ -322,1 +324,1 @@
-  if (!base::ReadFileToString(kApexLibrariesConfigFile, &file_content)) {
+  if (!darwin_art::filesystem::ReadGuestConfig(kApexLibrariesConfigFile, &file_content)) {
