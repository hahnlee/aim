[package]
license.workspace = true
name = "darwin-art-runtime"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["rlib", "staticlib"]

[dependencies]
libc = "0.2"
darwin-art-abi = { path = "../darwin-art-abi" }
darwin-art-engine-sys = { path = "../darwin-art-engine-sys" }
darwin-art-profile = { path = "../darwin-art-profile" }

[dev-dependencies]
bionic-fs-facade = { path = "../../tools/bionic-fs-facade" }
