package android.app.servertransaction;

import android.content.res.Configuration;
import android.os.IBinder;
import android.window.ActivityWindowInfo;

/** Compile-only Android 16 signature stub; runtime resolution uses framework.jar. */
public class ActivityConfigurationChangeItem extends ClientTransactionItem {
    public ActivityConfigurationChangeItem(IBinder activityToken, Configuration config,
            ActivityWindowInfo activityWindowInfo) {}
}
