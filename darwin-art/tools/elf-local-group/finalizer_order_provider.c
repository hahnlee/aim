// Link-only test observer declaration. The execution test supplies Record,
// not this stub image, through the loader's explicit virtual-provider contract.
void record_finalizer(int node) { (void)node; }
